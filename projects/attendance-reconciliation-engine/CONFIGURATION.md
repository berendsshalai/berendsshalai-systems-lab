# Configuration

Rules are configurable:

- `late_grace_minutes`
- `early_departure_grace_minutes`

Break rules, pay-period definitions and timezone handling are planned extension points. No legal or payroll thresholds are hard-coded as universal truth.
